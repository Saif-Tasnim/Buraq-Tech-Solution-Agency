/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.postimg.cc", "storage.googleapis.com" , "flowbite.s3.amazonaws.com"],
  },
};

export default nextConfig;

// i.postimg.cc
