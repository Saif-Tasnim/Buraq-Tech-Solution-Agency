import { PreviousWork } from '@/app/(public)/portfolio/component';
import React from 'react';

const Sample = () => {
    return (
        <div>
            <PreviousWork />
        </div>
    );
};

export default Sample;